import React from "react";
import { Post } from "containers/post";
interface Props {}

export default function PostPage(props: Props) {
    return (
        <div>
            <Post />
        </div>
    );
}
